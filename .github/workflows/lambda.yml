name: lambda

# Path-filtered so a site or Terraform change never redeploys the function.
# DECISIONS.md #21 — three workflows, one least-privilege role each.
on:
  push:
    branches: [main]
    paths:
      - 'lambda/**'
      - '.nvmrc'
      - '.github/workflows/lambda.yml'
  workflow_dispatch:

# Two deploys racing would leave whichever finished second as the live code,
# which may not be the newer commit. Queue instead of cancelling.
concurrency:
  group: lambda-deploy
  cancel-in-progress: false

permissions: {}

env:
  AWS_REGION: ap-southeast-1
  FUNCTION_NAME: joaqs-online-chat

jobs:
  deploy:
    name: package and publish
    runs-on: ubuntu-latest

    permissions:
      # Required to mint the OIDC token that AWS exchanges for credentials.
      id-token: write
      contents: read

    steps:
      # Every third-party action is pinned to a full commit SHA, not a tag. A
      # tag is a movable pointer, and this job holds credentials to a live AWS
      # account.
      - name: Checkout
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - name: Set up Node
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version-file: '.nvmrc'
          cache: npm
          cache-dependency-path: lambda/package-lock.json

      # --omit=dev keeps the package to what actually runs. `npm ci` rather
      # than `npm install`: it installs exactly what the lock file pins and
      # fails if the lock disagrees with package.json.
      - name: Install production dependencies
        working-directory: lambda
        run: npm ci --omit=dev

      # Node parses the handler before the runtime will invoke it, so a syntax
      # error caught here is a failed build instead of a 502 in front of a
      # recruiter.
      - name: Syntax check
        working-directory: lambda
        run: node --check index.mjs

      - name: Package
        working-directory: lambda
        run: |
          zip -qr ../function.zip index.mjs node_modules
          size=$(stat -c%s ../function.zip)
          echo "package: $((size / 1024)) kB"
          # 50 MB is the hard limit for a direct upload. Well under it today;
          # this fails loudly rather than erroring cryptically in the API call.
          [ "$size" -lt 50000000 ] || { echo "package too large for direct upload"; exit 1; }

      - name: Assume the deploy role
        uses: aws-actions/configure-aws-credentials@61815dcd50bd041e203e49132bacad1fd04d2708 # v5
        with:
          role-to-assume: arn:aws:iam::377510222046:role/joaqs-online-lambda-deploy
          role-session-name: lambda-deploy-${{ github.run_id }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Update the function code
        run: |
          aws lambda update-function-code \
            --function-name "$FUNCTION_NAME" \
            --zip-file fileb://function.zip \
            --query 'LastModified' --output text
          # The update is asynchronous: the API returns before the new code is
          # live. Publishing over a still-updating function fails, and so does
          # smoke-testing one.
          aws lambda wait function-updated-v2 --function-name "$FUNCTION_NAME"

      # A green deploy that leaves the assistant broken is worse than a red
      # one. This goes through CloudFront, so it exercises the whole path the
      # browser uses: the behaviour, the OAC signature, the function, the
      # corpus fetch and the model call.
      #
      # It retries for three minutes rather than failing on the first attempt.
      # A push touching both `lambda/**` and `site/**` starts this workflow and
      # `site.yml` at the same time, and they run in parallel. The corpus this
      # test proves is loaded is published by THAT workflow — so on such a push
      # (every first deploy, and any change to the site data) the assistant
      # legitimately answers without a corpus for a minute or two while the
      # other job builds and syncs. Failing immediately would make a red build
      # the normal outcome of a correct deploy.
      - name: Smoke test through CloudFront
        run: |
          question='{"messages":[{"role":"user","content":"What certifications does Luis hold?"}]}'
          # The /api/* origin is a Lambda function URL behind Origin Access
          # Control, so CloudFront signs the origin request with SigV4 and
          # Lambda refuses unsigned payloads: without the body hash every POST
          # is rejected 403 before the handler runs. The browser computes the
          # same header in ChatDialog.astro. A smoke test that omitted it would
          # fail on a perfectly good deploy.
          hash=$(printf '%s' "$question" | sha256sum | cut -d' ' -f1)
          deadline=$(( SECONDS + 180 ))
          attempt=0
          while :; do
            attempt=$(( attempt + 1 ))
            body=$(curl -sS -X POST https://joaqs.online/api/chat \
              -H 'content-type: application/json' \
              -H "x-amz-content-sha256: $hash" \
              -d "$question" --max-time 45 || true)
            # A reply naming a credential proves the whole path at once:
            # behaviour, OAC signature, function, corpus fetch and model call.
            if printf '%s' "$body" | grep -qE 'DOP-C02|SAA-C03|SOA-C03'; then
              echo "attempt $attempt: grounded answer received"
              printf '%s\n' "$body"
              exit 0
            fi
            if [ "$SECONDS" -ge "$deadline" ]; then
              echo "no grounded answer after $attempt attempts over 3 minutes:"
              printf '%s\n' "$body"
              echo "the assistant is answering without the corpus - failing the deploy"
              exit 1
            fi
            echo "attempt $attempt: not grounded yet, retrying in 15s"
            sleep 15
          done
